services:
  - type: web
    name: tindahan-tracker
    runtime: static
    buildCommand: "echo Static site ready"
    staticPublishPath: .
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: "true"
